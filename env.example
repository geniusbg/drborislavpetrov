# Database - PostgreSQL Configuration
DB_HOST=your-database-host
DB_PORT=5432
DB_NAME=your-database-name
DB_USER=your-database-user
DB_PASSWORD=your-database-password

# PostgreSQL Environment Variables (for backup functionality)
PGHOST=your-database-host
PGPORT=5432
PGDATABASE=your-database-name
PGUSER=your-database-user
PGPASSWORD=your-database-password

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=dr.petrov@example.com
ADMIN_EMAIL=admin@example.com

# Admin Authentication
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Admin API Tokens (comma-separated for multiple tokens)
ADMIN_TOKENS=mock-token,test,your-production-token

# Uploads (опционално: за production с Apache и външна папка)
# Ако е зададено, качванията отиват тук; Apache трябва да сервира /uploads от същия път
# UPLOADS_BASE_PATH=/var/www/uploads/drpetrov

# Security
JWT_SECRET=your-secret-key-here 